<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.sale.dal.mysql.productioncompeteinfo.ProductionCompeteInfoMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <resultMap id="base" type="cn.iocoder.yudao.module.sale.controller.admin.productioncompeteinfo.vo.ProductionCompeteInfoRespVO">
        <id column="id" property="id" />
        <result column="production_id" property="productionId" />
        <result column="production_name" property="productionName" />
        <result column="dept_id" property="deptId" />
        <result column="price" property="price" />
        <result column="create_time" property="createTime" />

    </resultMap>

    <select id="selectListByCondition"
            resultMap="base">
        SELECT t1.*, t2.name as production_name
        FROM sale_production_compete_info t1
                 left join sale_production_info t2
                           on t1.production_id = t2.id and t2.deleted = '0'
        where t1.deleted = '0'
            <if test="productionName != null">
                and t2.name like concat('%', #{productionName}, '%')
            </if>
    </select>
</mapper>