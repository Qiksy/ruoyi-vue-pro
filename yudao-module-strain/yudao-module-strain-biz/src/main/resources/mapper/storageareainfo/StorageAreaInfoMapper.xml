<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.strain.dal.mysql.storageareainfo.StorageAreaInfoMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="getSockStatus" resultType="java.util.Map" parameterType="arraylist">
        select status,count(1) num from strain_freezing_tube_stock_info where box_id in (
        /*查询这个区域下面的所有盒子*/
        select id from strain_freezing_device_hierarchy
        where freezing_device_id in
        <foreach collection="list" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        and is_final_level = 1
        and deleted = 0
        )
        group by status
    </select>
</mapper>
