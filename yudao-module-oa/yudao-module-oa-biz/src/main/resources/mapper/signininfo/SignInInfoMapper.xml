<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.oa.dal.mysql.signininfo.SignInInfoMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->


    <select id="selectPageByUserTable"
            resultType="cn.iocoder.yudao.module.oa.dal.dataobject.signininfo.SignInInfoDO">
        select t1.* from oa_sign_in_info t1
                 left join oa_sign_in_user t2 on t2.meeting_id = t1.id
        where
            t2.user_id = #{userId}
          and t2.deleted = 0
          and t1.deleted = 0
        <if test="req.title != null and req.title != ''">
            and t1.title like concat('%', #{req.title}, '%')
        </if>
        order by t1.start_date desc,t1.id desc
    </select>
</mapper>